# ============================================================
#  PneumoIA — Dépendances Python
#  Installer avec : pip install -r requirement.txt
# ============================================================

# ---------- API & Serveur ----------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # upload fichiers (multipart/form-data)

# ---------- Base de données ----------
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0                # driver PostgreSQL async
psycopg2-binary>=2.9.9         # driver PostgreSQL sync (migrations Alembic)

# ---------- Validation & Sérialisation ----------
pydantic>=2.7.0
pydantic-settings>=2.2.0       # lecture du .env

# ---------- Sécurité & Auth ----------
python-jose[cryptography]>=3.3.0   # JWT (HS256)
passlib[bcrypt]>=1.7.4             # hash passwords bcrypt
pyotp>=2.9.0                       # génération OTP 6 chiffres

# ---------- Email & SMS ----------
# Email : envoi SMTP natif Python (smtplib) — compatible Brevo, SendGrid, Mailtrap
twilio>=9.0.0                      # SMS (Twilio)

# ---------- Tâches asynchrones ----------
celery[redis]>=5.3.0               # workers async (emails, SMS, PDF)
redis>=5.0.0                       # broker Celery + cache OTP

# ---------- Stockage fichiers ----------
boto3>=1.34.0                      # AWS S3

# ---------- Génération PDF ----------
weasyprint>=62.0                   # PDF bilans de consultation

# ---------- Intelligence Artificielle ----------
scikit-learn>=1.5.0                # modèle de classification
joblib>=1.4.0                      # sérialisation/désérialisation modèle (.pkl)
shap>=0.45.0                       # explication des prédictions (SHAP values)
pandas>=2.2.0                      # manipulation des données
numpy>=1.26.0                      # calcul numérique
pillow>=10.0.0                     # redimensionnement photos profil

# ---------- Utilitaires ----------
python-dotenv>=1.0.0               # chargement .env en local
